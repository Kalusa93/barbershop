<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs', {title: 'Resultados de la busqueda' ,
  css:'products.css'}) %>

  <body>
    <%- include('./partials/header.ejs') %>

    <main>
      <section>
        <div class="products">
          <% products.forEach((product)=> { %>
          <a href="/products/detail/<%=product.id %>" class="main-article">
            <div class="image-article">
              <img
                src="/images/products/<%= product.image %>"
                alt="Catena Zapata"
              />
            </div>
            <div class="details-article">
              <h4><%= product.name %></h4>
              <p class="type">
                <strong>Tipo de corte: </strong>
                <%= product.type %>
              </p>
              <p class="uva">
                <strong>Peluquero: </strong>
                <%= product.barber %>
              </p>
            </div>
          </a>
          <% }) %>
        </div>
      </section>
    </main>

    <%- include('./partials/footer.ejs') %>
  </body>
</html>
